# CUTIE_STREET イベント自動整理Webアプリ 要件定義書

## 1. 概要
CUTIE_STREET_の公式Xアカウントの投稿からイベント告知を自動で取得し、  
時系列順に整理してWebアプリに表示するシステム。  
Claudeを利用して「投稿がイベント告知であるか」「重複していないか」を判断する機能を持つ。  
フロントはVercelでホスティングする。

---

## 2. 目的
- イベント告知の見逃しを防ぐ  
- 投稿が大量でも自動で整理・選別して表示  
- 無料枠を超えない範囲でデータ取得・保存・表示

---

## 3. 対象情報
- X（旧Twitter）のCUTIE_STREET_アカウント  
  - URL: [https://x.com/CUTIE_STREET_](https://x.com/CUTIE_STREET_)  
  - 投稿内容・日時・リンクを取得  
  - Claudeに投稿内容を渡し、イベント告知かどうかを判定

---

## 4. データ設計

### 4.1 保存対象
- 投稿ID（Xのstatus ID）
- 投稿日時
- 投稿テキスト
- 判定フラグ（Claudeによるイベント判定: true/false）
- Web表示フラグ（すでに表示済みか）
- 投稿URL

### 4.2 保存形式
- 無料枠を意識して **SQLiteまたはJSONファイル** を利用  
- データ量はCUTIE_STREET_の投稿数程度であれば無料枠内に収まる想定

### 4.3 データの更新ルール
- 新規投稿のみ取得  
- Claudeでイベント判定  
- 判定がtrueかつ未保存の場合のみDBに追加  
- DBは時系列順でソートしてフロントに返す

---

## 5. システム構成

### 5.1 バックエンド
- 言語: Python
- フレームワーク: FastAPI または Flask
- 機能:
  1. X API（Tweepyなど）でCUTIE_STREET_投稿取得  
  2. Claudeでイベント判定  
  3. データ保存（SQLite/JSON）  
  4. APIでフロントに時系列順のイベントデータを提供

### 5.2 フロントエンド
- 言語/フレームワーク: React / Next.js  
- ホスティング: Vercel  
- 機能:
  - APIから取得したイベントを一覧表示  
  - 日付順にソート  
  - 投稿タイトル、日時、リンクを表示

### 5.3 自動更新
- バックエンドで定期取得（Python scheduleライブラリ）  
- 例: 1日1回の取得（無料枠内を想定）  

---

## 6. データフロー

1. X APIで最新投稿を取得  
2. Claudeに投稿本文を渡し「イベントかどうか判定」  
3. 判定がtrueかつDBに存在しない場合のみ保存  
4. DBを時系列順にソートしてAPIで返す  
5. フロントがAPIを呼び出して一覧表示  

---

## 7. 無料枠対策
- X APIの無料枠を超えないよう、取得はCUTIE_STREET_のみ  
- 過去投稿は一定期間（例: 30件分）だけ保持  
- Claude利用回数も未保存・未判定投稿のみに限定  

---

## 8. 将来的拡張
- LINE/メール通知機能  
- Instagram連携（無料枠の範囲で）  
- カレンダー自動登録  
- 投稿本文のキーワードによるカテゴリ分け  
- kawaii.Lab所属の他グループのxアカウントでの告知も拾えるようにする

---

## 9. 開発ステップ

1. X APIでCUTIE_STREET_投稿取得 → JSON保存  
2. Claudeで「イベントかどうか」判定  
3. DB/JSONに保存し重複回避  
4. FastAPI/FlaskでAPI提供  
5. React/Next.js + Vercelで一覧表示  
6. 定期取得スクリプトで自動更新  

---

## 10. 備考
- Claude利用は無料枠で抑える  
- Vercelはフロント無料枠で運用可能  
- データ件数が増えた場合は古い投稿を削除して無料枠を維持