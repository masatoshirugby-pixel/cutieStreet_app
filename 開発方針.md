# CUTIE_STREET イベント自動整理Webアプリ 開発方針

## 1. 技術スタック

| レイヤー | 技術 | 理由 |
|----------|------|------|
| フロントエンド | Next.js (React) | Vercel との親和性が高く、SSR/SSG で高速表示 |
| バックエンド | Python + FastAPI | 非同期処理が得意、軽量でAPI構築に最適 |
| AI判定 | Claude API (Anthropic) | イベント告知判定・重複チェックに使用 |
| データ保存 | SQLite | 無料枠内で運用、ファイルベースで管理が簡単 |
| ホスティング（フロント） | Vercel | 無料枠で運用可能 |
| ホスティング（バックエンド） | Render / Railway（無料枠） | FastAPI をデプロイ |
| X投稿取得 | Tweepy (X API v2) | 無料枠内で特定アカウントの投稿を取得 |

---

## 2. ディレクトリ構成

```
cutieStreet_app/
├── backend/
│   ├── main.py              # FastAPI エントリーポイント
│   ├── x_fetcher.py         # X API 投稿取得
│   ├── claude_judge.py      # Claude によるイベント判定
│   ├── db.py                # SQLite 操作
│   ├── scheduler.py         # 定期取得スクリプト
│   ├── models.py            # データモデル定義
│   ├── requirements.txt
│   └── events.db            # SQLite データベース
└── frontend/
    ├── app/
    │   ├── page.tsx         # イベント一覧ページ
    │   └── layout.tsx
    ├── components/
    │   └── EventCard.tsx    # イベント表示カード
    ├── lib/
    │   └── api.ts           # バックエンドAPIクライアント
    ├── package.json
    └── next.config.ts
```

---

## 3. 開発ステップ

### Step 1: バックエンド基盤構築
- FastAPI プロジェクト初期化
- SQLite テーブル設計・作成
  - `events` テーブル: `id`, `post_id`, `post_text`, `post_url`, `posted_at`, `is_event`, `created_at`
- 依存パッケージ: `fastapi`, `uvicorn`, `tweepy`, `anthropic`, `sqlite3`（標準）, `schedule`

### Step 2: X API 投稿取得
- Tweepy で `CUTIE_STREET_` アカウントの最新投稿を取得
- 取得件数は最大30件（無料枠を考慮）
- 取得済みの `post_id` はスキップ（重複防止）

### Step 3: Claude によるイベント判定
- 取得した投稿テキストを Claude API に渡す
- プロンプト例：「以下の投稿はアイドルグループのイベント告知ですか？ true/false で答えてください。」
- `true` の場合のみ DB に保存

### Step 4: FastAPI エンドポイント実装
- `GET /events` — 時系列順のイベント一覧を返す
- CORS 設定（Vercel フロントからのアクセスを許可）

### Step 5: 定期取得スクリプト
- `schedule` ライブラリで1日1回 Step 2〜3 を自動実行
- バックグラウンドプロセスとして動作

### Step 6: フロントエンド構築
- Next.js プロジェクト初期化（`create-next-app`）
- `GET /events` を呼び出してイベント一覧を表示
- EventCard コンポーネントで日時・テキスト・リンクを表示
- 日付順ソート（バックエンドで処理済み）

### Step 7: Vercel デプロイ
- `frontend/` を Vercel にデプロイ
- 環境変数: `NEXT_PUBLIC_API_URL`（バックエンドのURL）

---

## 4. 環境変数

```env
# backend/.env
X_BEARER_TOKEN=xxxx          # X API Bearer Token
ANTHROPIC_API_KEY=xxxx       # Claude API Key

# frontend/.env.local
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
```

---

## 5. 無料枠制約と対策

| サービス | 無料枠の制約 | 対策 |
|----------|-------------|------|
| X API | 月500リクエスト（Basic無料枠） | 1日1回取得、対象アカウント1件のみ |
| Claude API | 月の無料クレジット | 未判定の投稿のみ判定、判定済みはスキップ |
| Vercel | フロント無料 | そのまま利用 |
| バックエンドホスティング | Render/Railway 無料枠 | スリープあり（許容範囲） |

---

## 6. 開発優先順位

1. **バックエンド先行**（X取得 → Claude判定 → DB保存 → API提供）
2. **フロントエンド**（APIから取得して表示）
3. **定期取得の自動化**
4. **Vercel + Render デプロイ**

---

## 7. 将来拡張への備え

- DB スキーマに `category` カラムを最初から追加（キーワードカテゴリ分けに備える）
- アカウント名をハードコードせず設定ファイル化（他グループ対応のため）
- 通知機能は別モジュール（`notifier.py`）として分離しておく
