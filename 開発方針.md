# イベント自動整理Webアプリ 開発方針

最終更新: 2026-03-25

---

## 1. 技術スタック

| レイヤー | 技術 | 備考 |
|---|---|------|
| フロントエンド | Next.js 14 App Router | ISR revalidate=300秒 |
| フロントホスティング | Vercel | GitHub連携で自動デプロイ |
| バックエンド | Python + FastAPI | 非同期処理・スクレイピング |
| バックホスティング | Railway | PostgreSQL も同環境 |
| データベース | PostgreSQL（Railway） | SQLiteから移行済み |
| Webスクレイピング | requests + BeautifulSoup | asobisystem系サイト対応 |
| イベント判定 | キーワードマッチング + fugashi | Claude APIは不使用（コスト削減） |
| X投稿取得 | Tweepy (X API v2) | 課金対象。実行前に要確認 |
| メール取得 | IMAP（Gmail） | 申込締切情報の補完用 |

---

## 2. ディレクトリ構成

```
cutieStreet_app/
├── backend/
│   ├── main.py           # FastAPI エントリーポイント
│   ├── scheduler.py      # パイプライン制御・定期実行ループ
│   ├── web_fetcher.py    # 公式サイトスクレイピング
│   ├── x_fetcher.py      # X API 投稿取得
│   ├── email_fetcher.py  # Gmail IMAP メール取得
│   ├── claude_judge.py   # イベント判定・カテゴリ分類
│   ├── event_utils.py    # 日付/会場/締切抽出・重複検出
│   ├── db.py             # PostgreSQL 操作
│   └── models.py         # データモデル定義
└── frontend/
    ├── app/
    │   ├── page.tsx              # トップページ（グループ選択）
    │   └── [group]/page.tsx      # グループ別カレンダーページ
    ├── components/
    │   └── Calendar.tsx          # カレンダーコンポーネント
    └── lib/
        └── api.ts                # APIクライアント・型定義
```

---

## 3. 開発ルール

### コスト管理
- **X API は課金対象**。手動実行（`POST /fetch`）前に必ずユーザーに確認する
- Claude API は現在不使用（キーワードマッチングで代替済み）

### デプロイフロー
```
git push → GitHub → Vercel 自動デプロイ（フロント）
                  → Railway 自動デプロイ（バック）
```
- Vercel のデータキャッシュは revalidate=300 により最大5分で更新
- コード変更は push 後に自動反映。バッチ結果は最大5分後に反映

### ブランチ運用
- `main`: 本番（CUTIE STREET / CANDY TUNE / SWEET STEADY 専用アプリ）
- `saas-prototype`: アソビシステム系グループ横展開SaaSの試作（開発中）

---

## 4. 環境変数

```env
# Railway（バックエンド）
DATABASE_URL=postgresql://...
X_BEARER_TOKEN=xxxx
ANTHROPIC_API_KEY=xxxx（現在未使用）
GMAIL_ADDRESS=xxxx
GMAIL_APP_PASSWORD=xxxx

# Vercel（フロントエンド）
NEXT_PUBLIC_API_URL=https://cutiestreetapp-production.up.railway.app
```

---

## 5. 今後の予定

| 優先度 | 内容 |
|---|---|
| 高 | Vercel Deploy Hook 設定（バッチ後の即時キャッシュクリア） |
| 中 | SaaSプロトタイプ（`saas-prototype` ブランチ）の開発・検証 |
| 低 | 通知機能（LINE / メール） |
