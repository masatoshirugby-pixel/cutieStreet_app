# アイドルグループ イベント自動整理Webアプリ 要件定義書

最終更新: 2026-03-25

---

## 1. 概要

CUTIE_STREET_ / CANDY_TUNE_ / SWEET_STEADY の3グループを対象に、
公式スケジュールページ・ニュースページ・Xアカウント・Gmailからイベント情報を自動取得し、
グループ別カレンダーに表示するWebアプリ。

---

## 2. 対象グループ

| グループ名 | Xアカウント | 公式サイト（スケジュール） |
|---|---|---|
| CUTIE_STREET_ | @CUTIE_STREET_ | cutiestreet.asobisystem.com |
| CANDY_TUNE_ | @CANDY_TUNE_ | candytune.asobisystem.com |
| SWEET_STEADY | @SWEET_STEADY | sweetsteady.asobisystem.com |

---

## 3. データソース

### 3.1 公式スケジュールページ（主）

- URL: `https://{group}.asobisystem.com/live_information/schedule/list/`
- **今月 + 翌月** のイベントを全件取得
- `<a href="/live_information/detail/...">` および `<a href="/news/detail/...">` を解析
  - スケジュールページのリンク先が `/news/detail/` の場合も取得対象（例: TV出演告知）
- 毎パイプライン実行時に全削除→再取得（申込締切レコードは保持）
- イベントタイプをカテゴリに直接マッピング（claude_judge 不使用）

| スケジュールTYPE | カテゴリ |
|---|---|
| LIVE（ワンマン・単独公演キーワードあり） | 単独ライブ |
| LIVE（フェス・festival キーワードあり） | フェス出演 |
| LIVE（合同・対バン キーワードあり） | 合同ライブ |
| LIVE（上記以外） | ライブ |
| EVENT（大特典会キーワードあり） | 大特典会 |
| EVENT（オンラインサイン会キーワードあり） | オンラインサイン会 |
| EVENT（リリイベ・インストアキーワードあり） | リリースイベント |
| EVENT（特典会・チェキ・お渡しキーワードあり） | 特典会 |
| EVENT（フェス・festival キーワードあり） | フェス出演 |
| EVENT（上記以外） | その他イベント |
| TV | テレビ出演 |
| RADIO | ラジオ出演 |
| VIDEO | **スキップ** |

### 3.2 X（Twitter）（副）

- 毎日 08:00 UTC に過去24時間分を差分取得
- スケジュールページに**ない**イベントのみカレンダーに追加（重複排除）
- claude_judge（キーワードマッチング + fugashi形態素解析）でイベント判定・カテゴリ分類

### 3.3 ニュースページ

- URL: `https://{group}.asobisystem.com/news/1/`（1ページ目・2ページ目）
- 詳細ページを全件取得し、claude_judge でイベント判定
- 差分取得（`is_post_exists` でスキップ）

### 3.4 Gmail（補助）

- IMAP で申込締切メールを取得しカレンダーに「申込締切」として表示
- 楽天・注文確認系メールはフィルタリングして除外

---

## 4. イベント処理ルール

### 4.1 重複排除

- **スケジュールイベント**: 重複チェックなし（公式情報として全件保存）
- **X / ニュースイベント**: スケジュール保存後に重複チェック
  - 同カテゴリ × 同日（date_match_threshold=0）→ 重複とみなしスキップ
  - 日付なし → Jaccard類似度 ≥ 0.35 → 重複とみなしスキップ

### 4.2 イベント日付抽出（6パターン対応）

| パターン | 例 |
|---|---|
| YYYY年M月D日 | 2026年3月21日 |
| YYYY/M/D | 2026/3/21 |
| YYYY.MM.DD | 2026.03.07 |
| M月D日 | 3月21日 |
| M/D（スペース可） | 3/21、3 /29 |
| MM DD [DAY] | 03 01 [SUN] |

- 年省略時: 90日以上前なら翌年と判定
- 5年以上前の日付はノイズとして除外
- 開催日キーワード（「日時:」「開催日:」等）周辺の日付を優先抽出（ニュース記事の公開日ノイズ対策）

### 4.3 申込締切の抽出・保存

- 以下のキーワード直後の日付を申込締切として抽出:
  - 申込締切 / 受付締切 / 応募期間 / 〆切 / シリアル番号発番期間 / 先行受付 / 一次受付・二次受付 等
- 日付範囲（A～B）は末尾（終了日）を締切とみなす
- 複数締切がある場合は個別レコードとして保存（post_id: `{親ID}_deadline`, `{親ID}_deadline_1`, ...）
- `venue` カラムに締切ラベル（例: "応募期間", "一次受付"）を格納

### 4.4 イベント有効期限

- 通常イベント（申込締切以外）: `event_date` から **14日後** に自動削除
- **申込締切レコード**: 親イベントが DB に存在する間は削除しない（締切日が過去でも保持）
  - 親イベントが削除されてから14日後に削除
- スケジュールイベントは毎回全削除→再取得のため期限に依存しない

### 4.5 バックフィル

- DB 内でまだ締切レコードが未作成のイベントに対して、詳細ページを再取得して締切を補完
- 対象: `source IN ('news', 'x', 'web')` かつ `_deadline` レコードなし
- エンドポイント: `POST /fetch/deadlines`

---

## 5. システム構成

### 5.1 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | Next.js 14 App Router（ISR revalidate=**300**秒） |
| フロントホスティング | Vercel |
| バックエンド | Python + FastAPI |
| バックホスティング | Railway |
| データベース | PostgreSQL（Railway） |
| Webスクレイピング | requests + BeautifulSoup |
| イベント判定 | キーワードマッチング + fugashi形態素解析 |

### 5.2 ディレクトリ構成

```
cutieStreet_app/
├── backend/
│   ├── main.py           # FastAPI エントリーポイント・APIエンドポイント
│   ├── scheduler.py      # パイプライン制御・定期実行ループ
│   ├── web_fetcher.py    # 公式サイトスクレイピング（スケジュール・ニュース）
│   ├── x_fetcher.py      # X API 投稿取得
│   ├── email_fetcher.py  # Gmail IMAP メール取得
│   ├── claude_judge.py   # イベント判定・カテゴリ分類（X・ニュース用）
│   ├── event_utils.py    # 日付抽出・会場抽出・締切抽出・重複検出ユーティリティ
│   ├── db.py             # PostgreSQL 操作
│   └── models.py         # データモデル定義
└── frontend/
    ├── app/
    │   ├── page.tsx              # トップページ（グループ選択）
    │   └── [group]/page.tsx      # グループ別カレンダーページ
    ├── components/
    │   └── Calendar.tsx          # カレンダーコンポーネント
    └── lib/
        └── api.ts                # バックエンドAPIクライアント・型定義
```

### 5.3 データベーススキーマ（主要テーブル）

**events テーブル**

| カラム | 型 | 説明 |
|---|---|---|
| post_id | TEXT UNIQUE | スケジュール: 詳細ページURL / X: ツイートID / 締切: `{親ID}_deadline[_N]` |
| post_text | TEXT | イベント本文（newsは最大3000文字） |
| post_url | TEXT | ソースリンク |
| posted_at | TEXT | 投稿/取得日時 |
| is_event | BOOLEAN | イベント判定フラグ |
| account | TEXT | グループ識別子 |
| category | TEXT | イベントカテゴリ |
| event_date | TEXT | 開催日 / 締切日（ISO形式） |
| venue | TEXT | 会場名 / 締切ラベル |
| image_url | TEXT | 画像URL |
| source | TEXT | データソース（'web' / 'x' / 'news' / 'email'） |
| created_at | TEXT | レコード作成日時 |

**emails テーブル**

| カラム | 型 | 説明 |
|---|---|---|
| message_id | TEXT UNIQUE | GmailメッセージID |
| account | TEXT | グループ識別子 |
| subject | TEXT | 件名 |
| sender | TEXT | 送信者 |
| received_at | TEXT | 受信日時 |
| body_preview | TEXT | 本文プレビュー |
| deadline_date | TEXT | 抽出した締切日 |
| created_at | TEXT | レコード作成日時 |

---

## 6. APIエンドポイント

| メソッド | パス | 説明 |
|---|---|---|
| GET | /events | イベント一覧取得（account, limit パラメータ） |
| GET | /emails | メール一覧取得（account パラメータ） |
| GET | /health | ヘルスチェック |
| POST | /fetch | フルパイプライン手動実行（Web + X + Email） |
| POST | /fetch/web | Webスクレイピングのみ手動実行（X API不使用） |
| POST | /fetch/deadlines | 申込締切バックフィル実行 |

---

## 7. パイプライン実行スケジュール

- **起動時**: Webパイプラインのみ即時実行（X API不使用）
- **定期実行**: 毎日 08:00 UTC（17:00 JST）にフルパイプライン実行
- **実行順序**: Web（スケジュール + ニュース）→ X → Email → 締切バックフィル → 期限切れ削除
  - Webを先に処理することでXイベントの重複排除が機能する

---

## 8. フロントエンド機能

### 8.1 カレンダー表示

- 月ナビゲーション（前月・翌月）
- 月曜始まり、土曜=青・日曜=赤
- カテゴリフィルター（複数選択可）
- イベントはカテゴリ別カラーで表示

| カテゴリ | 色 |
|---|---|
| 大特典会 | アンバー |
| 特典会 | イエロー |
| オンラインサイン会 | ティール |
| 単独ライブ | ピンク（濃） |
| 合同ライブ | ピンク |
| フェス出演 | ライム |
| ライブ | ピンク（中） |
| リリースイベント | パープル |
| テレビ出演 | ブルー（濃） |
| ラジオ出演 | バイオレット |
| 雑誌掲載 | シアン |
| その他メディア | ブルー（淡） |
| 配信イベント | グリーン |
| 物販・グッズ | オレンジ |
| 販売・発売 | オレンジ（濃） |
| 申込締切 | レッド |
| 当選通知 | イエロー（濃） |
| その他イベント | グレー |

### 8.2 イベント詳細パネル（右パネル）

- カレンダーのイベントをクリックで右パネルに表示
- 表示内容: カテゴリ / 先着順バッジ / 開催日 / 会場 / 申込締切一覧 / 画像 / 本文 / ソースリンク
- `source = 'web'` の場合は投稿日時を非表示
- ソースリンク: X投稿 → `𝕏 元の投稿を見る` / 公式サイト → `🌐 公式ページを見る`

### 8.3 申込締切の紐づけ表示

- 親イベント選択時: 対応する締切レコードを「📅 申込締切」として右パネルに一覧表示
- 締切バッジ選択時: 対応する親イベント情報を右パネルに表示
- **紐づけロジック**: `deadline.post_id.startsWith(event.post_id + "_deadline")` で完全一致
- 選択時: カレンダー上で紐づくバッジを赤リングでハイライト
- 「申込締切」フィルター選択時: 対応する親イベントも合わせてカレンダーに表示

### 8.4 先着順表示

- `post_text` に「先着」が含まれる場合、カレンダーバッジに「（先着）」を付記
- 右パネルに「先着順」グリーンバッジを表示

### 8.5 日程未確定イベント

- `event_date` なしのイベントはカレンダーに表示せず
- カレンダー下部の「▶ 日程未確定 N件」をクリックで展開表示

---

## 9. 今後の課題・予定

- Vercel Deploy Hook によるバッチ完了後の即時キャッシュクリア（現在は revalidate=300 で対応中）
- 通知機能（LINE / メール）
- アソビシステム系他グループへの横展開（SaaSプロトタイプとして別ブランチで開発中）
