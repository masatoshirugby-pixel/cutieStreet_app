# アイドルグループ イベント自動整理Webアプリ 要件定義書

最終更新: 2026-03-22

---

## 1. 概要

CUTIE_STREET_ / CANDY_TUNE_ / SWEET_STEADY の3グループを対象に、
公式スケジュールページおよびXアカウントからイベント情報を自動取得し、
グループ別カレンダーに表示するWebアプリ。

---

## 2. 対象グループ

| グループ名 | Xアカウント | 公式サイト（スケジュール） |
|---|---|---|
| CUTIE_STREET_ | @CUTIE_STREET_ | cutiestreet.asobisystem.com |
| CANDY_TUNE_ | @CANDY_TUNE_ | candytune.asobisystem.com |
| SWEET_STEADY | @SWEET_STEADY（※） | sweetsteady.asobisystem.com |

---

## 3. データソース

### 3.1 公式スケジュールページ（主）

- URL: `https://{group}.asobisystem.com/live_information/schedule/list/`
- **今月 + 翌月末** までのイベントを全件取得
- `<a href="/live_information/detail/...">` タグを直接解析（詳細ページへのアクセスなし）
- 毎パイプライン実行時に全削除→再取得（最新状態を反映）
- イベントタイプをカテゴリに直接マッピング（claude_judge 不使用）

| スケジュールTYPE | カテゴリ |
|---|---|
| LIVE | ライブ |
| EVENT（握手・チェキ・特典会キーワードあり） | 握手会・チェキ会 |
| EVENT（リリースイベント・インストアキーワードあり） | リリースイベント |
| EVENT（上記以外） | その他イベント |
| TV / RADIO | メディア出演 |
| VIDEO | **スキップ** |

### 3.2 X（Twitter）（副）

- 毎日 08:00 UTC に過去24時間分を差分取得
- スケジュールページに**ない**イベントのみカレンダーに追加（重複排除）
- claude_judge（キーワードマッチング + fugashi形態素解析）でイベント判定
- イベント日付・会場を正規表現で抽出

### 3.3 ニュースページ（一時停止中）

- URL: `https://{group}.asobisystem.com/news/1/` の詳細ページ
- 現在コメントアウト中。再有効化時は `web_fetcher.py` の `NEWS_URLS` を戻す

### 3.4 Gmail（補助）

- IMAP で申込締切メールを取得しカレンダーに「申込締切」として表示
- 楽天等の注文確認メールはフィルタリングして除外

---

## 4. イベント処理ルール

### 4.1 重複排除

- **スケジュールイベント**: 重複チェックなし（公式情報として全件保存）
- **Xイベント**: スケジュール保存後に重複チェック
  - 同カテゴリ × 日付±3日以内 → 重複とみなしスキップ
  - 日付なし → Jaccard類似度 ≥ 0.35 → 重複とみなしスキップ

### 4.2 イベント日付抽出（6パターン対応）

| パターン | 例 |
|---|---|
| YYYY年M月D日 | 2026年3月21日 |
| YYYY/M/D | 2026/3/21 |
| YYYY.MM.DD | 2026.03.07 |
| M月D日 | 3月21日 |
| M/D（スペース可） | 3/21、3 /29 |
| MM DD [DAY] | 03 01 [SUN] |

- 年省略時: 90日以上前なら翌年と判定
- 5年以上前の日付はノイズとして除外
- タイトル部分（冒頭200文字）を優先

### 4.3 イベント有効期限

- `event_date` あり: イベント日から **14日後** に自動削除
- `event_date` なし: `posted_at` から14日後に自動削除
- スケジュールイベントは毎回全削除→再取得のため有効期限に依存しない

### 4.4 グループ間の独立性

- 3グループのイベントは account 単位で完全に独立して管理
- 同じイベント（合同イベント等）もグループ別に別レコードとして保存
- post_id はドメイン込みの完全URL（グループ間の衝突なし）

---

## 5. システム構成

### 5.1 技術スタック

| レイヤー | 技術 |
|---|---|
| フロントエンド | Next.js 14 App Router（ISR revalidate=3600） |
| フロントホスティング | Vercel |
| バックエンド | Python + FastAPI |
| バックホスティング | Railway |
| データベース | PostgreSQL（Railway） |
| Webスクレイピング | requests + BeautifulSoup |
| イベント判定 | キーワードマッチング + fugashi形態素解析 |

### 5.2 ディレクトリ構成

```
cutieStreet_app/
├── backend/
│   ├── main.py           # FastAPI エントリーポイント・APIエンドポイント
│   ├── scheduler.py      # パイプライン制御・定期実行ループ
│   ├── web_fetcher.py    # 公式サイトスクレイピング
│   ├── x_fetcher.py      # X API 投稿取得
│   ├── email_fetcher.py  # Gmail IMAP メール取得
│   ├── claude_judge.py   # イベント判定（Xイベント用）
│   ├── event_utils.py    # 日付抽出・会場抽出・重複検出ユーティリティ
│   ├── db.py             # PostgreSQL 操作
│   └── models.py         # データモデル定義
└── frontend/
    ├── app/
    │   ├── page.tsx          # トップページ（グループ選択）
    │   └── [group]/page.tsx  # グループ別カレンダーページ
    ├── components/
    │   └── Calendar.tsx      # カレンダーコンポーネント
    └── lib/
        └── api.ts            # バックエンドAPIクライアント・型定義
```

### 5.3 データベーススキーマ（主要テーブル）

**events テーブル**

| カラム | 型 | 説明 |
|---|---|---|
| post_id | TEXT UNIQUE | スケジュール: 詳細ページURL / X: ツイートID |
| post_text | TEXT | イベント本文 |
| post_url | TEXT | ソースリンク（Xまたは公式ページURL） |
| posted_at | TEXT | 投稿/取得日時 |
| is_event | BOOLEAN | イベント判定フラグ |
| account | TEXT | グループ識別子 |
| category | TEXT | イベントカテゴリ |
| event_date | TEXT | 開催日（ISO形式） |
| venue | TEXT | 会場名 |
| image_url | TEXT | 画像URL |
| source | TEXT | データソース（'web' / 'x' / 'email'） |
| created_at | TEXT | レコード作成日時 |

---

## 6. APIエンドポイント

| メソッド | パス | 説明 |
|---|---|---|
| GET | /events | イベント一覧取得（account, limit パラメータ） |
| POST | /fetch | フルパイプライン手動実行（X + Web + Email） |
| POST | /fetch/web | Webスクレイピングのみ手動実行（X API不使用） |
| GET | /emails | メール一覧取得 |
| GET | /health | ヘルスチェック |

---

## 7. パイプライン実行スケジュール

- **起動時**: 即時実行（フルパイプライン）
- **定期実行**: 毎日 08:00 UTC（17:00 JST）
- **実行順序**: Web（スケジュール）→ X → Email → 期限切れ削除
  - Webを先に処理することでXイベントの重複排除が機能する

---

## 8. フロントエンド機能

### 8.1 カレンダー表示

- 月ナビゲーション（前月・翌月）
- 月曜始まり、土曜=青・日曜=赤
- イベントはカテゴリ別カラーで表示

| カテゴリ | 色 |
|---|---|
| ライブ | ピンク |
| リリースイベント | パープル |
| 握手会・チェキ会 | イエロー |
| メディア出演 | ブルー |
| 配信イベント | グリーン |
| 物販・グッズ | オレンジ |
| その他イベント | グレー |
| 申込締切 | レッド |
| 販売・発売 | オレンジ（濃） |

### 8.2 イベント詳細パネル

- カレンダーのイベントをクリックで右パネルに表示
- 表示内容: カテゴリ / 開催日 / 会場 / 画像 / 本文 / ソースリンク
- ソースリンク: X投稿 → `𝕏 元の投稿を見る` / 公式サイト → `🌐 公式ページを見る`（post_url が空の場合は非表示）

### 8.3 日程未確定イベント

- event_date なしのイベントはカレンダーに表示せず
- カレンダー下部の「▶ 日程未確定 N件」をクリックで展開表示

---

## 9. 今後の課題・予定

- ニュースページ（`/news/1/`）パイプラインの再有効化
- カレンダーへのソースリンク表示（既存DBレコードのpost_url補完）
- 通知機能（LINE / メール）
